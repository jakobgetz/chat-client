import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  footer: {
    position: "absolute",
    bottom: 0,
    padding: "10px",
    display: "flex",
    alignItems: "center",
  },
  sendButton: {
    margin: "10px",
  },
});
