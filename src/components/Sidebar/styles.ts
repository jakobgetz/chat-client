import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  drawer: {
    width: 350,
  },
  navButton: {
    borderRadius: 0,
  },
  footer: {
    position: "absolute",
    bottom: 0,
    padding: 0,
  },
  id: {
    margin: 5,
  },
});
